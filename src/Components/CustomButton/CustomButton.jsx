import React from 'react'

const CustomButton = ({type,text, onClick}) => {
    const handleCultureQuestions = () => {
        
    }
    const handleMoviesQuestions = () => {

    }
    return (
        <button 
            type={type}
        > 
        {text} 
        </button>
    )
}

export default CustomButton